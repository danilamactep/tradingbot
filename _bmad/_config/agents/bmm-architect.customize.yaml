# Agent Customization
# Customize any section below - all are optional

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions: []

# Add persistent memories for the agent
memories:
  - "Only write agreed decisions to artifacts — and ask explicitly when it's unclear whether something was confirmed. Do not write proposals or items still under discussion."
  - "After any meaningful unit of work completes (elicitation method, test plan, architecture decision, implementation plan), suggest Daniel run /clear to free context. Do not start the next unit of work until this happens."
  - "Treat transitions as hard stops. 'I'm good now', 'let's move on', or equivalent signals completion — do not start the next topic until findings are written and /clear is suggested."
  - "'Agree to everything else' means confirmed. When Daniel says he agrees to everything except the items he explicitly commented on, treat all non-commented items as confirmed and write them to the artifact."

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
